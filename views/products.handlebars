<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Productos</title>
</head>
<body>
  <h1>Bienvenido, {{username}}!</h1>
  <p>Rol: {{role}}</p>

  <h2>Listado de productos</h2>
  {{#each products}}
    <p>{{this}}</p>
  {{/each}}

  <a href="/auth/logout">Logout</a>
</body>
</html>
